# AGENTS.md — Codex 作業指示書

このファイルは、Codexが本プロジェクトで作業するときの進め方、検証方法、完了条件を定めるものです。

画面、機能、文章、デザイン、動作、データ構造に関する正式な仕様は `SPEC.md` を参照してください。
ユーザーから別途指示がない限り、実装判断では最新の `SPEC.md` を優先します。

---

## 基本方針

- 依頼された範囲を正確に実装する
- 作業前に `SPEC.md` の該当箇所と関連コードを読む
- 既存の挙動を必要以上に変更しない
- 仕様にない機能、デザイン、文言、依存関係を独自判断で追加しない
- 依頼と無関係なリファクタリングを同時に行わない
- 既存の共通処理、型、コンポーネント、データ定義を再利用する
- 一時的な回避策ではなく、原因に対応した修正を行う
- 変更内容を説明できないコードを追加しない

---

## 作業プロセス

### 1. 仕様を確認する

作業開始前に、`SPEC.md` の該当箇所を確認します。

個別の修正依頼では、その依頼と必要な影響範囲だけを変更します。
`SPEC.md` 全体への対応を明示的に依頼された場合のみ、既存実装と仕様全体を比較して対応します。

仕様の一部だけを読んで判断せず、関連する共通仕様、アクセシビリティ、レスポンシブ、データ管理、完了条件も確認してください。

### 2. 関連ファイルを読む

変更前に、少なくとも以下を確認します。

- 変更対象ファイル
- 呼び出し元と呼び出し先
- 関連する型定義、定数、データ
- 同様の処理を行う既存実装
- 関連するテスト
- 設定ファイルと実行スクリプト

ファイル名やコンポーネント名だけから動作を推測せず、実際の内容を確認してください。

### 3. 影響範囲を確認する

実装前に、変更が次へ与える影響を確認します。

- 他のページ、コンポーネント、共通レイアウト
- 型定義とデータ構造
- URL、ルーティング、クエリパラメータ
- フォームとAPI間の送受信データ
- SEO、メタデータ、構造化データ
- レスポンシブ表示
- キーボード操作とアクセシビリティ
- テーマ、状態管理、タイマー、ブラウザAPI
- 既存テストとビルド

削除対象がある場合は、ページ本体だけでなく、リンク、型、データ、API、環境変数参照、import、テスト、設定の残存も確認します。

### 4. 必要に応じて `plan.md` を作成・更新する

以下に該当する作業では、実装前に `plan.md` を作成または更新します。

- 複数のページや機能にまたがる変更
- 多数のファイルを変更する作業
- 共通コンポーネント、データ構造、APIを変更する作業
- 既存機能の移行、統合、廃止を含む作業
- 実装方法に複数の選択肢がある作業
- 段階的な対応が必要な作業

`plan.md` には以下を記載します。

- 対応する仕様
- 実装方針
- 変更予定のファイルと理由
- 影響範囲
- 検証方法
- 懸念点、制約、未確定事項

単純な文言修正や局所的なスタイル修正では、`plan.md` の作成は不要です。
`plan.md` は仕様書の代わりに使用しません。

### 5. 実装する

実装時は以下を守ります。

- 既存の命名規則とコードスタイルに合わせる
- 既存の責務分割を確認し、適切な場所へ実装する
- 同じ意味の処理やデータを重複して持たない
- 仕様上の表示順を無断で変更しない
- マジックナンバーや固定文字列は、共有が必要な場合に定数化する
- 不要な依存関係を追加しない
- 使用されていないコード、import、型、データを残さない
- デバッグ用ログ、仮リンク、仮データ、仮画像参照を残さない
- コメントは処理内容の説明ではなく、判断理由や制約を補足する場合に使用する
- エラーを握りつぶすだけの処理を追加しない
- クライアント側だけの検証をセキュリティ境界として扱わない
- ユーザー入力をHTMLへ埋め込む場合は適切にエスケープする
- シークレット値や個人情報をクライアントコード、出力、ログへ含めない

### 6. テストを追加・更新する

新しいロジック、不具合修正、データ変換、状態遷移には、可能な範囲で再発防止用のテストを追加します。

既存テストの期待値を変更する場合は、実装結果に合わせるのではなく、`SPEC.md` と依頼内容に基づいて正しい期待値を設定してください。

`tests/`、`__tests__/`、テスト用fixtureなどの専用領域が存在する場合は、その役割を維持します。本体実装をテスト専用領域へ配置しません。

### 7. 自動検証を行う

変更後は、`package.json` などの設定を確認し、プロジェクトに定義されている検証コマンドを実行します。

変更内容に応じて、利用可能な範囲で以下を確認します。

- 関連テスト
- 全体テスト
- lint
- 型チェック
- production build

コマンド名は推測せず、実際の設定を確認してください。
存在しないコマンドを、依頼なしに新設する必要はありません。

検証が失敗した場合は原因を確認し、今回の変更に起因する問題は修正します。
既存不具合や環境依存で修正できない場合は、失敗内容と切り分け結果を報告します。

### 8. 手動確認を行う

自動テストだけで確認できない変更は、対象に応じて手動確認します。

例:

- 指定された画面幅での表示
- 横スクロール、はみ出し、レイアウト移動の有無
- 内部リンク、外部リンク、戻る・進む操作
- URLクエリと表示状態の同期
- キーボード操作、Escキー、フォーカス移動、フォーカスリング
- フォームの入力、送信中、成功、失敗、リセット
- 画像、動画、モーダル、アニメーション
- テーマ、時間帯、特殊状態、ページ再読み込み
- `prefers-reduced-motion` 時の挙動
- SEOメタデータ、canonical、OGP、構造化データ
- 削除対象へのリンクや参照が残っていないこと

実行環境の制約で確認できない項目は、未確認として完了報告に記載します。

### 9. 差分を見直す

完了前に変更差分を確認し、以下を点検します。

- 依頼範囲外の変更が混入していないか
- 意図しない整形や大量変更がないか
- 不要なファイルが追加されていないか
- 削除した機能の参照が残っていないか
- 仮実装、TODO、デバッグコードが残っていないか
- 仕様とテストが一致しているか

---

## 直接編集しないもの

明示的な指示がない限り、以下は直接編集しません。

- ビルド生成物
- キャッシュ
- 依存パッケージの実体
- 自動生成ファイル
- カバレッジ出力
- テスト実行結果や一時ファイル
- 環境変数の実値
- シークレット情報
- ロックファイル。ただし依存関係を変更した場合を除く

生成結果を変える必要がある場合は、生成元のコードまたは設定を修正します。

---

## 仕様の不一致や問題を発見した場合

以下を発見した場合は、独自判断で大規模な仕様変更を行いません。

- `SPEC.md` 内の矛盾
- 依頼内容と `SPEC.md` の矛盾
- 既存機能と両立しない指定
- 実装不可能な指定
- セキュリティ上の問題
- データ消失や後方互換性破壊の可能性
- 依頼範囲を大きく超える変更の必要性

安全に対応可能な範囲は実装し、対応しなかった内容、理由、影響を完了報告に記載します。
推測だけで仕様を補完しないでください。

---

## 完了条件

以下を満たした時点で作業完了とします。

- 依頼された変更が実装されている
- `SPEC.md` の該当仕様と一致している
- 関係のない既存機能を変更していない
- 削除対象の参照や不要コードが残っていない
- 関連テストが成功している
- 型エラーがない
- lintエラーがない
- production buildが成功している
- 必要な手動確認が完了している
- デバッグコードや仮実装が残っていない
- 未実施事項と既知の問題が明記されている

プロジェクトに該当する検証コマンドが存在しない場合や、実行環境上確認できない場合は、その項目を未実施として報告します。
実行していない検証を成功として報告しないでください。

---

## 完了報告

作業完了時は、以下の形式で報告します。

```md
## 変更サマリー

### 対応内容
- 実装または修正した内容
- 削除または移行した内容

### 変更したファイル
- `path/to/file` — 変更内容と理由
- `path/to/test` — 追加または更新したテスト

### 検証結果
- 関連テスト: 成功 / 失敗 / 未実施
- 全体テスト: 成功 / 失敗 / 未実施
- lint: 成功 / 失敗 / 未実施
- 型チェック: 成功 / 失敗 / 未実施
- build: 成功 / 失敗 / 未実施
- 手動確認: 実施内容または未実施理由

### 未実施・要確認
- 未実施項目
- 確認できなかった条件
- 既知の問題
- 別作業として必要な対応
```

変更していないファイルや、実行していない検証を完了済みとして報告しないでください。
