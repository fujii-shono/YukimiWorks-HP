## 対応する仕様

- `SPEC.md` 全体
- 特にトップページ再現、共通サイトフレーム、時間帯テーマ、各一覧・詳細ページ、問い合わせフォーム、SEO、アクセシビリティ
- 加えて、最新依頼によるトップページ構成変更、About の番号整列、キャラクター会話ロジック全面更新、時間帯イベント仕様変更、不在イベント追加

## 実装方針

- Next.js 14 App Router + TypeScript を新規構成する
- `yukimiworks-html-sample` の構造と寸法を基準に、トップページの見た目を CSS と再利用コンポーネントへ移植する
- サイト全体は `SiteFrame` を共通化し、ヘッダー、サイドバー、フッター、右カラム差し替えで全ページを構成する
- 時間帯テーマと特殊イベントは共通 hook / provider / データ定義で管理し、開発環境のみデバッグ UI とクエリ強制表示を有効にする
- キャラクター会話は時間帯別メッセージ、短時間連打判定、睡眠状態、不在イベントを含む状態機械として再設計する
- データ駆動を優先し、`works`、`news`、`links`、`portfolio`、`appServices` を配列管理して一覧と詳細へ反映する
- 問い合わせフォームは `react-hook-form` + `zod` + API Route + `lib/mailer.ts` で実装する
- サンプルフォルダの CSS / JS / 画像は直接参照せず、必要素材を `public` 配下へ移して利用する
- 依頼内容で仕様と異なる点は `SPEC.md` も合わせて更新する

## 変更予定のファイルと理由

- `package.json` ほか設定一式: Next.js / TypeScript / lint / build の実行基盤
- `app/**`: ルーティング、各ページ、API Route、グローバル CSS
- `components/**`: 共通レイアウト、レトロパネル、各ページ UI、テーマ UI、フォーム UI
- `data/**`: ページ表示とテーマ制御の元データ
- `lib/**`: 日本時間判定、バリデーション、メール送信
- `public/**`: ロゴ、キャラクター、アイコン、背景、ダミー画像などの実運用素材
- `next.config.js` または `next.config.mjs`: Cloudinary 画像許可など
- `SPEC.md`: 新しいトップ構成、会話仕様、イベント仕様の正式化

## 影響範囲

- 全ページのレイアウト、レスポンシブ、SEO メタデータ
- テーマ切り替えとイベント時の配色、アイコン、背景
- トップページのパネル順序と追加ポートフォリオプレビュー
- About 本文の番号表示形式
- キャラクターの画像差し替え、クリック無反応条件、吹き出し出現条件、連打判定
- 一覧ページのフィルターと URL クエリ同期
- 詳細ページの静的生成と構造化データ
- 問い合わせフォームの入力、送信、モーダル、API 応答
- 開発環境限定 UI の出し分け

## 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- `npm run dev` で主要ページ、モバイル幅、テーマ切り替え、フォーム送信 UI、キャラクター演出を手動確認

## 懸念点・制約・未確定事項

- 既存コードがほぼ存在しないため、実装は新規構築が中心になる
- 依存関係の取得にはネットワークアクセスが必要になる可能性が高い
- OGP 画像とファビコンは仕様に合わせた静的素材を新規作成する
- `SPEC.md` は今回の依頼で更新対象になるため、依頼差分のみ明示的に反映する

---

## 追加対応: 募金Webhook保存

### 対応する仕様

- ユーザー依頼: Stripe入金後の募金メッセージ保存を、Thanksページ到達ではなくWebhookで行う

### 実装方針

- Stripe Checkout Session 作成は既存の `/api/bokin/checkout` を維持する
- Stripe の `checkout.session.completed` Webhook を `/api/bokin/webhook` で受け取る
- `STRIPE_WEBHOOK_SECRET` と `stripe-signature` をHMACで検証し、Stripe由来のイベントだけ処理する
- 決済済み、完了済み、募金用metadataを満たすCheckout SessionだけRedisへ保存する
- 既存の processed session キーで二重保存を防止する
- `/bokin/thanks` は決済済みセッション確認とおみくじ表示に限定する

### 変更予定のファイルと理由

- `app/api/bokin/webhook/route.ts`: Stripe Webhook 受信と署名検証、保存処理
- `app/bokin/thanks/page.tsx`: Redis保存処理を削除し表示専用にする
- `README.md`: 本番環境変数とStripe Webhook endpoint設定を追記

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- Stripe CLIまたはStripeダッシュボードのテスト送信でWebhook到達とRedis保存を確認する

---

## 追加対応: カウンター実装

### 対応する仕様

- `SPEC.md` の Counter / Since パネル
- ユーザー追加依頼: 1 ブラウザ 1 日 1 回だけ計測し、Vercel 本番設定を README に記載する

### 実装方針

- カウンター総数は Upstash Redis に保存する
- クライアント側は `localStorage` に東京日付キーを保持し、その日未送信のときだけ `/api/counter` へ加算リクエストを送る
- Redis 未設定環境では `data/siteConfig.ts` の初期値を返して動作を壊さない

### 変更予定のファイルと理由

- `app/api/counter/route.ts`: 現在値取得と加算 API
- `lib/counter.ts`: Redis REST アクセスと初期値・表示整形の共通化
- `components/layout/Sidebar.tsx`: 表示更新と 1 日 1 回送信ロジック
- `README.md`: Redis の接続手順と環境変数設定
- `SPEC.md`: カウンター仕様の正式化

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`

---

## 追加対応: アクキーシミュレーター可変生成パラメータと回帰比較

### 対応する仕様

- ユーザー依頼: 本来のアクキーシミュレーターへの影響をなくしつつ、デモページ用に API へ固定値ではなく指定値を渡せるようにする
- ユーザー依頼: 変更前後の 3D 情報・SVG 情報を保存し、差異があるファイルを自動で表示する
- ユーザー依頼: 差異があっても NG とはせず、最終判断は人間に任せる
- ユーザー依頼: テスト用イラストを後から追加できるフォルダを用意する

### 実装方針

- 現在 API 内にある固定値を共有デフォルト設定として定義する
- 既存シミュレーターは共有デフォルト設定を明示的に API へ送る
- API は設定未指定時も同じデフォルトを使い、既存呼び出しとの後方互換性を保つ
- 回帰確認スクリプトは `tests/acrylic/illustrations/` 配下の PNG を自動で対象にする
- baseline は変更前保存用、compare は変更後の再生成・差分表示用に分ける

### 変更予定のファイルと理由

- `lib/acrylicGenerationOptions.ts`: 共有デフォルト設定と API 入力の正規化を定義するため
- `components/works/AcrylicKeychainTool.tsx`: 既存ツールから固定デフォルト値を明示的に送るため
- `app/api/acrylic/preview/route.ts`: 可変設定を受け取り、未指定時はデフォルトで生成するため
- `app/api/acrylic/export/route.ts`: SVG生成にも可変設定を受け取り、未指定時はデフォルトで生成するため
- `tests/acrylic/illustrations/.gitkeep`: 回帰確認用 PNG の格納場所を用意するため
- `scripts/acrylic-regression.mjs`: 変更前後のプレビュー JSON と SVG を保存・比較するため
- `package.json`: 回帰確認スクリプトの npm script を追加するため

### 影響範囲

- 既存ページ `/works/acrylic-keychain-tool` の API 呼び出し
- `/api/acrylic/preview` と `/api/acrylic/export` の request body
- サーバー側のマスク生成、穴生成、アクスタ台座・ツメ生成
- 生成結果の自動比較フロー

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- テスト用 PNG 追加後に `npm run acrylic:baseline` と `npm run acrylic:compare`

### 懸念点・制約・未確定事項

- 現時点では回帰確認用 PNG が未投入のため、比較スクリプトはフォルダだけ確認し、実比較は画像追加後に行う
- 差分検出は失敗扱いにせず、差分があるファイル名を表示して人間が判断する

---

## 追加対応: アクキー側面の3D厚みレイヤー

### 対応する仕様

- ユーザー依頼: アクキー本体の厚み表現を、アクスタ土台と同じレイヤー表現で追加する
- ユーザー依頼: 側面レイヤーの色はアクスタ土台と同じ薄いグレーにする

### 実装方針

- 既存の `edge` / `back` / `highlight` レイヤーは維持する
- `/api/acrylic/preview` で透明アクリル本体のシルエットを薄いグレーにした `sideSrc` を追加生成する
- フロントでは `sideSrc` をステージサイズへ整形し、前面と背面のZ位置の間に複数枚並べて厚みとして表示する
- 背面が正対しているときは側面帯のopacityを落とし、背面ハイライトに暗い帯がかぶらないようにする
- SVG/export は今回の表示改善対象外のため変更しない

### 変更予定のファイルと理由

- `app/api/acrylic/preview/route.ts`: アクキー/アクスタ共通のプレビュー側面レイヤーを返すため
- `components/works/AcrylicKeychainTool.tsx`: 側面レイヤーをcanvas整形し、3D空間へ複数枚配置するため
- `app/globals.css`: 側面レイヤーのZ位置・見た目を定義するため
- `scripts/acrylic-regression.mjs`: 新しい `sideSrc` レイヤーも比較成果物に含めるため

### 影響範囲

- `/works/acrylic-keychain-tool` のプレビュー表示
- `/api/acrylic/preview` のレスポンス形状
- 回帰比較で保存されるプレビューレイヤー成果物

### 検証方法

- `npm run lint`
- `npx tsc --noEmit --pretty false`
- `npm run build`
- `npm run acrylic:compare`
- 通常ブラウザで回転時に薄いグレーの側面厚みが見えることを確認する

### 懸念点・制約・未確定事項

- baseline には `sideSrc` が存在しないため、今回の比較では新規レイヤーが missing として表示される想定
- VS Code のブラウザプレビューではCSS 3Dが正しく見えない可能性があるため、表示確認は通常ブラウザを基準にする

---

## 追加対応: アクスタ台座・ツメ形状の専用確認

### 対応する仕様

- ユーザー依頼: アクスタ用のツメ形状についても、アクキー同様に変更で変わらないことを確認したい
- ユーザー依頼: 差分があってもNGにはせず、人間が最終判断できるようにする

### 実装方針

- 既存のアクキー回帰比較とは別に、アクスタの台座・ツメだけを確認する npm script を追加する
- `tests/acrylic/illustrations/` 配下の PNG を自動対象にする
- `keychain.clearRadius` を複数値に振り、アクスタの `standBaseFrame` と主要PNGレイヤーのハッシュが変わらないか確認する
- 目視確認用のPNGとJSONは `tests/acrylic/current/stand-claw-check/` に保存し、コミット対象にはしない

### 変更予定のファイルと理由

- `scripts/acrylic-regression.mjs`: `stand-claw-check` モードを追加するため
- `package.json`: `npm run acrylic:stand-claw-check` を追加するため
- `tests/acrylic/README.md`: 確認手順と出力先を明記するため

### 影響範囲

- テスト用スクリプトと npm script のみ
- アプリ本体の生成処理、通常ページ、デモページには影響しない

### 検証方法

- `ACRYLIC_TEST_BASE_URL=http://127.0.0.1:3006 npm run acrylic:stand-claw-check`
- `npm run lint`
- `npx tsc --noEmit --pretty false`
- `npm run build`

---

## 追加対応: アクキー/アクスタ デモページ

### 対応する仕様

- ユーザー依頼: デモページではサンプル画像を切り替えて、アクキー/アクスタの生成結果を確認できるようにする
- ユーザー依頼: デモページには画像読み込み機能を入れず、サンプル画像フォルダへ後から追加できる構成にする
- ユーザー依頼: アクキーは穴の位置とキャラクター周りのアクリル余白をモーダルで変更して再生成できるようにする
- ユーザー依頼: アクスタは台座幅とツメの横幅をモーダルで変更して再生成できるようにする
- ユーザー依頼: アクスタの台座高さとツメ位置は固定し、モーダルから設定不要にする
- ユーザー依頼: アクスタ平面プレビューにも、シンプル/安定それぞれの実生成アウトラインを表示する
- ユーザー依頼: 穴あり/穴なし、シンプル/安定の切替は維持する
- ユーザー依頼: SVG生成とAI生成ボタンを追加し、変更値をAPIへ渡せるようにする
- ユーザー依頼: デモページは導線を置かず、直リンクのみでアクセスする

### 実装方針

- 既存の `AcrylicKeychainTool` を props で通常モード/デモモードに分岐し、通常ページのデフォルト挙動は維持する
- デモモードでは `public/works/acrylic-keychain-tool-demo/samples/` 配下のサンプル一覧をページから渡す
- デモモードではサンプル選択時に公開PNGを取得して `File` として既存生成処理へ渡す
- 生成オプションは既存 `DEFAULT_ACRYLIC_GENERATION_OPTIONS` を初期値にし、デモモードだけユーザー変更値をAPIへ送る
- 余白変更は外側キャンバス余白ではなく、実生成のアクリル膨張量 `clearRadius` として扱う
- アクスタの台座高さ、最小高さ、奥行き、ツメ位置は送信時にもデフォルト値へ固定する
- アクスタ平面プレビューのアウトラインは、現在のシンプル/安定モードで生成済みの `edgeSrc` / `acrylicSrc` を重ねて表示する
- アクスタのツメはアウトライン膨張の対象にせず、底面位置に合わせて指定幅・指定長さの専用マスクとして合成する
- アクスタの台座横幅は `baseWidthPx` のピクセル指定を優先し、未指定時だけ従来の比率指定へフォールバックする
- アクスタのツメ横幅は `clawWidthPx` のピクセル指定を優先し、未指定時だけ従来の自動計算/比率指定へフォールバックする
- アクスタのツメ高さは `clawLengthPx` の固定ピクセル指定を優先し、未指定時だけ従来の台座奥行き由来/比率指定へフォールバックする
- 通常シミュレーターでは従来どおりツメ幅を自動計算し、デモでは初回アクスタ生成だけ従来の自動計算に任せ、返却された `standClawFrame.width` をpx指定値として保存して再調整に使う
- アクスタにもキャラクター周りの余白設定を追加し、API側でも `clearRadius` をアクスタ本体の外周余白として使う
- アクスタ平面プレビューは生成済みの `edgeSrc` / `acrylicSrc` を重ねず、元イラストと調整値に追従する余白輪郭線、台座、ツメだけで表示する
- 平面プレビューの余白ガイドは塗り面ではなく、元イラストのアルファから作った細い輪郭線として表示する
- 3Dプレビューと平面プレビューの台座位置は、どちらもAPIが返す `standBaseFrame.contactY` を基準にして揃える
- アクスタ平面プレビューの余白輪郭線は、フロント側で再計算せず、APIが実生成に使った `clearMask` から作った `standShapeGuide` を表示して底面特殊処理とのズレを防ぐ
- AI生成は現時点では専用APIを新規作成し、受け取った値を検証して未実装レスポンスを返す

### 変更予定のファイルと理由

- `components/works/AcrylicKeychainTool.tsx`: デモ用サンプル選択、設定モーダル、SVG/AI生成ボタンを追加するため
- `app/works/acrylic-keychain-tool-demo/page.tsx`: 直リンク用のデモページを追加するため
- `app/api/acrylic/ai-generate/route.ts`: AI生成ボタンが変更値をAPIへ渡せる受け口を追加するため
- `public/works/acrylic-keychain-tool-demo/samples/`: デモ用サンプルPNGを格納するため
- `app/globals.css`: デモ用UIと設定モーダルの表示を追加するため

### 影響範囲

- 通常ページ `/works/acrylic-keychain-tool` は props 未指定のため、ファイル選択と固定デフォルト値送信を維持する
- `/api/acrylic/preview` と `/api/acrylic/export` は既存の generationOptions 受け口を利用し、新規破壊的変更はしない
- Works一覧、作品データ、サイトナビゲーションにはデモページ導線を追加しない

### 検証方法

- `npm run lint`
- `npx tsc --noEmit --pretty false`
- `npm run build`
- 通常ページ側でデモ専用UIが出ないことをコード差分で確認する
- 可能であればローカルサーバーでデモページ表示とサンプル切替、設定変更後の再生成を確認する

### 懸念点・制約

- AI生成の中身は未実装のため、API受け口とボタン動作のみ作成する
- サンプルを追加する場合は、現時点ではデモページ内のサンプル配列にも追加が必要

---

## 追加対応: メッセージパネル

### 対応する仕様

- ユーザー依頼: `sample/message-modal-sample.html` のメッセージモーダルを実装する
- ユーザー依頼: メインページのみ初期状態で開く
- ユーザー依頼: 閉じた状態の色はテーマに合わせ、通知数表示は不要
- ユーザー依頼: モーダルを廃止し、左サイドバーの Counter 上に小さなスクロールパネルとして配置する
- ユーザー依頼: 折りたたみ機能をなくし、Counter タイトルの両側に装飾マークを揃える
- ユーザー依頼: Message パネルだけ周囲を角丸にし、名前を非表示にして、アイコンと分単位の日時を設定可能にする
- ユーザー依頼: 当日分は「○分前」「○時間前」、過去分は日付のみで表示する
- ユーザー依頼: アイコンは円形に切り抜き、日時は日本時間の簡単な形式で編集できるようにする
- ユーザー依頼: 投稿に任意画像を設定できるようにし、一覧では右側に小さく表示する
- ユーザー依頼: 投稿クリックでツールチップを開き、アイコンと時間、メッセージ、画像の順に表示する
- ユーザー依頼: ツールチップの吹き出し形状を維持し、公開時刻が未来の投稿はその時間まで表示しない

### 実装方針

- `Sidebar` で `What's New` と `Counter` の間に共通メッセージパネルを表示する
- サンプルの投稿表示を、固定モーダルではなく通常フローの小型スクロールパネルへ移植する
- 配色は既存の `--site-*` CSS 変数を使い、時間帯テーマとイベントテーマに追従させる
- 投稿 author は廃止し、アイコンと `publishedAt` を任意の投稿メタデータとして扱う
- 投稿画像は `image` として任意設定し、一覧ではサムネイル、ツールチップでは大きめに表示する
- `publishedAt` は `YYYY-MM-DD HH:mm` の日本時間文字列として扱い、データ編集時にタイムゾーン指定を不要にする
- 日付表示はブラウザ側で現在時刻を参照し、東京日付が当日なら相対表示、それ以外は日付表示にする
- 現在時刻より未来の `publishedAt` を持つ投稿は描画対象から除外し、分単位の再判定で公開する
- ツールチップ外枠は吹き出しの三角を表示できるよう `overflow` を持たせず、中身だけスクロールさせる

### 変更予定のファイルと理由

- `data/messages.ts`: 表示メッセージをデータ化するため
- `components/ui/MessagePanel.tsx`: メッセージパネル本体を実装するため
- `components/layout/Sidebar.tsx`: メッセージパネルを Counter 上へ配置し、Counter タイトル装飾を揃えるため
- `app/globals.css`: サンプル形状を既存テーマ変数でスタイル化するため

### 影響範囲

- 全ページ左サイドバー内のメッセージ表示
- モバイル時のパネル表示順
- Counter タイトルバーの装飾表示

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- トップページとサブページでメッセージパネル配置、縦スクロール、Counter タイトル装飾を手動確認

---

## 追加対応: 深夜警告中のリンク制御

### 対応する仕様

- `SPEC.md` の深夜警告イベント
- ユーザー依頼: 深夜警告状態では `/` 以外の遷移先をトップページへ集約する

### 実装方針

- 深夜警告状態を参照する共通リンクコンポーネントを追加し、通常のページ遷移リンクは `/` 以外なら `/` へ向け替える
- すでに `/` 以外のページにいる状態で深夜警告へ切り替わった場合や直接アクセスした場合に備えて、共通レイアウトで `/` へ戻すガードを追加する
- ページ内アンカーや `mailto:` などの非ページ遷移リンクは対象外にして、アクセシビリティと基本操作を維持する

### 変更予定のファイルと理由

- `components/ui/RestrictedLink.tsx`: 深夜警告時の遷移先制御を共通化するため
- `components/theme/SleepWarningRouteGuard.tsx`: 深夜警告中の非トップページ滞在を防ぐため
- `components/layout/SiteFrame.tsx`: 全ページへガードを適用するため
- `app/**`, `components/**`: 既存のページ遷移リンクを共通リンクへ置き換えるため

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`

---

## 追加対応: カウンター記念番号モーダル

### 対応する仕様

- ユーザー依頼: 訪問者番号が `10000` ごとの場合は「記念すべき〇〇番目！めっちゃめでたい！」を表示し、クラッカー風エフェクトを出す
- ユーザー依頼: 訪問者番号が `1000` ごとの場合は「あなたは〇〇番目の訪問者です！おめでとう！」を表示する
- ユーザー依頼: 訪問者番号が全桁同じゾロ目の場合は「なんとゾロ目！すごいね！」を表示する
- ユーザー依頼: 優先順位は `10000ごと > 1000ごと > ゾロ目` とする
- ユーザー依頼: モーダルは番号を踏んだ本人だけに表示し、メッセージと右上の `×` ボタンのみで構成する
- ユーザー依頼: 一度モーダルを出したら Redis / Upstash にフラグを保存し、新しい記念番号判定までは再表示しない
- ユーザー依頼: スクロール判定関連は既存実装済みのため変更しない

### 実装方針

- Counter の `POST /api/counter` で増加した訪問者番号に対してのみ記念番号を判定する
- Redis 未設定時は永続フラグを保存できないため、記念番号モーダルは返さず既存カウンター表示のみ維持する
- Redis に最後に表示した記念番号を保存し、同じ番号以下では再表示しない
- クライアントは `POST` 応答に含まれる記念番号情報だけをモーダル表示し、`GET` では表示しない
- 10000 ごとの記念番号のみ、モーダル表示時に CSS のクラッカー風エフェクトを重ねる

### 変更予定のファイルと理由

- `lib/counter.ts`: Redis の記念番号判定・表示済みフラグ保存を追加するため
- `app/api/counter/route.ts`: `POST` 応答へ記念番号情報を含めるため
- `components/layout/Sidebar.tsx`: 記念番号モーダルとクラッカー風エフェクトを表示するため
- `app/globals.css`: 専用モーダルとクラッカー風エフェクトのスタイルを追加するため

### 影響範囲

- 全ページ共通のサイドバー Counter
- `POST /api/counter` の JSON 応答
- Redis / Upstash の Counter 関連キー

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`

---

## 追加対応: Hanabi 形つき花火

### 対応する仕様

- ユーザー依頼: 花火の中心黄色をオレンジ寄りにし、猫・うさぎ・ハート・星の形を赤・オレンジ・ピンクでランダム表示する
- ユーザー依頼: 形つき花火は専用ボタンを押している間のみ出す
- ユーザー依頼: 形花火ボタンを押せるようにし、粒数を増やして迫力を出す
- ユーザー依頼: デフォルトで花火は2発ずつ、位置を離して出す
- ユーザー依頼: 2発の花火のタイミングをランダムで少しずらす
- ユーザー依頼: 形花火の別色粒は内側ではなく、メインの花火に混ぜて配置する
- ユーザー依頼: 小さな花火2つと、中央の大きな爆発後に下へ進み残像が残る花火パターンを追加する
- ユーザー依頼: 形花火を含む追加パターンは押している間ではなく、押すたびにオン/オフする
- ユーザー依頼: 花火モード中は `highlight-1.png` と `hair-highlight-1.png` を常に消す
- ユーザー依頼: 残像花火は一番高い場所に巨大な白っぽいものを置き、左右の小花火は通常オレンジにする
- ユーザー依頼: 残像花火は最後にキラキラさせず、下へなだらかに落ちる残像にする
- ユーザー依頼: 残像花火だけは3つ同時に打ち上げる
- ユーザー依頼: 通常の2発は高さ差を広げ、片方を比較的下に表示する
- ユーザー依頼: 残像花火の左右小花火はかなり下げた位置に表示する
- ユーザー依頼: 残像花火左右サイズをベースに、多色の小花火3発と白い直線残像粒を出すパターンを追加する
- ユーザー依頼: 通常の2発は左ばかり高くならないよう高さをランダムにし、多色花火は少し上目に配置する
- ユーザー依頼: 多色花火の白い帯は、中心からゆっくり残像を残して花火より少し大きめに伸び、ゆっくり消えるようにする
- ユーザー依頼: 通常花火は上寄りにし、片方は必ずオレンジ、もう片方は多色花火と同様の色を左右交互に出す
- ユーザー依頼: 形花火も左右交互にし、色は多色花火と同じパレットから両方ランダムにする
- ユーザー依頼: 多色花火は中心が上寄り、左右が真ん中寄りになるようにし、タイミングをさらにずらす
- ユーザー依頼: 赤・黄色・オレンジ・ピンク・紫・青の小花火を連続で打ち上げ、同時に3つ以上の爆発が存在しない新パターンを追加する
- ユーザー依頼: 連発花火の大きさは通常花火と同じにする
- ユーザー依頼: 連発花火は通常花火同様に残し、タイミング次第では4つ出てもよい
- ユーザー依頼: 連発花火は花火玉の上昇を描写せず、間隔を開けてランダムに爆発だけを出し続ける
- ユーザー依頼: 連発花火は3つを少しランダムにずらして連続で出し、その3つが消える前に次の3つを爆発させる
- ユーザー依頼: 残像花火と多色花火の左右花火をもう少し端に出す
- ユーザー依頼: 通常連発に多色花火を混ぜる15秒、多色2つ+中央形花火15秒、短間隔2発連発10秒、残像花火、白い2発連発10秒、巨大虹色花火のフィナーレパターンを追加する
- ユーザー依頼: 終幕前半2パートは同じ間隔で少し間隔を開け、残像花火後は消えるまで次を出さず、巨大虹色花火後は停止する
- ユーザー依頼: 巨大虹色花火は粒数を変えず、外側を虹色にして1つだけ出す
- ユーザー依頼: 終幕の各パートは粒がすべて消えてから少し間隔をあけて次へ進む
- ユーザー依頼: 終幕内の間隔が違う連続花火は通常の連続花火へ変更する
- ユーザー依頼: 最後の虹色花火前も、その前の花火が消えるまで少し待つ

### 実装方針

- 既存の通常花火モードは維持する
- 形つき花火ボタンの押下状態を React state で保持し、打ち上げ時だけ粒の生成パターンを切り替える
- 形状は canvas 粒の速度ベクトルを形状点から作り、既存の描画・消滅処理を再利用する
- 打ち上げは2発単位で左右に離した位置を生成する
- 形花火ボタンは pointer / mouse / touch 入力を拾い、押下中 state が分かる表示にする
- 2発目は短いランダム遅延を持たせ、同時すぎる見え方を避ける
- 形花火は外側色と差し色をパレットとして選び、差し色粒をメイン形状の粒へ混ぜる
- 追加パターンは排他的なトグルとして管理し、通常花火モードとは独立させる
- 残像花火は小型花火2発と中央落下粒子を1バッチで生成し、中央粒子のみ残像情報を持たせる
- 通常ハイライト2枚は花火モード中だけ opacity で非表示にする
- 残像花火は中央大玉だけ白系専用パレットを使い、左右の小花火は通常オレンジのパレットを使う
- 残像粒子は点滅を無効化し、長めの残像と強めの重力で下へ流す
- 残像花火の小2発と中央大玉は launchDelay を 0 にして同時に打ち上げる
- 通常/形の2発は上寄りと下寄りの targetY を生成して高さ差を出す
- 残像花火の左右小花火は画面高さの約35〜48%付近で開かせる
- 多色花火は赤・黄色・オレンジ・ピンク・紫・青のパレットから3発分を選び、対応する差し色を中心粒に使う
- 多色花火には非点滅の白い残像粒を全方向へ直線的に飛ばす粒群を混ぜる
- パターンボタンは3つになるため下段に並べ、花火モード切替は少し上段へ配置する
- 通常/形の2発は低い側を左右ランダムに割り当て、片側固定の高さ偏りを避ける
- 多色花火は targetY を上寄りにし、白い残像粒は低速・長寿命・長めの trailDuration にする
- 通常/形の2発は高い側を左右交互に割り当て、通常花火はオレンジ側も左右交互にする
- 形花火と通常花火のランダム色側は多色花火と同じパレットを使う
- 多色花火は中央1発を上寄り、左右2発を中段寄りにして、launchDelay を大きめにずらす
- 連発花火は `rapid` パターンとして追加し、ロケットを生成せず直接爆発を追加する
- 連発花火は3発セットを予約キューに積み、セット内は短いランダム間隔でずらして爆発させる
- 連発花火の次セットは前セットの粒が残っている間に始まるよう、約1.9〜2.5秒後に予約する
- 連発花火は通常花火と同じ burstScale、粒数、寿命を使う
- 残像花火と多色花火の左右 targetX は画面幅の約8〜22%、78〜92%へ寄せる
- フィナーレは `finale` パターンとして追加し、直接爆発キューで時間フェーズごとに花火を予約する
- フィナーレ開始時はパターン切替バージョンで開始時刻と予約キューを初期化する
- フィナーレ前半2パートは同じ3発セット間隔で少し余白を持たせる
- 残像花火後は描画中のロケット・爆発・粒・予約がなくなるまで待ってから白花火へ進む
- 巨大虹色花火は専用粒生成で粒数を固定し、外周粒へ虹色パレットを巡回適用する
- フィナーレ終端後は `finaleComplete` にして追加花火を停止する
- フィナーレは絶対経過時間ではなくステップ制御にし、各ステップの発火終了後に全粒子消滅と待機時間を確認して次へ進める
- フィナーレ内の連続花火は通常の3発セット間隔を再利用し、白花火も同じ間隔で出す

### 変更予定のファイルと理由

- `components/portfolio/HanabiScene.tsx`: 形つき花火の粒生成と押下中 state を追加するため
- `app/globals.css`: 形つき花火ボタンの配置と状態表示を追加するため

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`

---

## 追加対応: デバッグ用キリ番表示とスマホ表示順

### 対応する仕様

- ユーザー依頼: デバッグメニューにキリ番判定を出すボタンを追加する
- ユーザー依頼: デバッグメニューでは `10000ごと`、`1000ごと`、`ゾロ目` の3種類を出せるようにする
- ユーザー依頼: ポートフォリオ画面や成果物画面など、スマホ版ではデスクトップのメインコンテンツを Menu の下に表示する
- ユーザー依頼: スクロール判定関連は変更しない

### 実装方針

- デバッグボタンは実カウンターや Redis を更新せず、クライアントイベントで3種類の記念番号モーダルだけを表示する
- スマホ時は `.main-column` 直下の通常コンテンツを `Menu` 直下の order に変更する
- トップページ内の `Portfolio` / `Works` / `Link` など既存の個別 order は維持する

### 変更予定のファイルと理由

- `components/theme/ThemeDebugPanel.tsx`: デバッグメニューへキリ番表示ボタンを追加するため
- `components/layout/Sidebar.tsx`: デバッグイベントを受け取って記念番号モーダルを表示するため
- `app/globals.css`: スマホ時のメインコンテンツ表示順を調整するため

### 影響範囲

- 開発時のデバッグメニュー
- スマホ幅の全ページ共通レイアウト順序

### 検証方法

- `npm run lint`
- `npx tsc --noEmit --incremental false`
- `npm run build`

---

## 追加対応: ポートフォリオ HTML 作品

### 対応する仕様

- ユーザー依頼: `public/portfolio/hikage.png` を使い、時間帯で背景色と装飾が切り替わる HTML 作品を追加する

### 実装方針

- `data/portfolio.ts` に HTML 作品として 1 件追加する
- `components/portfolio/` に専用レンダラーを追加し、夜間は暗い背景と月・星、昼間は白背景と下部の十字星を描画する
- 粒と月は背景 canvas / 前面 canvas に分けて描画し、`hikage.png` と同じ矩形フレーム内に収めてはみ出しを防ぐ
- 粒データは定数化し、数・位置・サイズ・点滅タイミングを手で調整しやすい形で管理する
- ぼかし、影、発光アニメーションは使わず、粒ごとの単純なフェードだけに絞ってスマホ負荷を下げる
- HTML 作品は画像と違って自然なサイズを持たないため、一覧とモーダルで専用の表示枠を必ず与える
- `fill` と `height: 100%` の連鎖に頼らず、作品側が `aspect-ratio` か `width` / `height` を持つようにする

### 変更予定のファイルと理由

- `data/portfolio.ts`: 作品データを登録するため
- `components/portfolio/PortfolioHtmlComponents.tsx`: HTML 作品レンダラーの登録先として使うため
- `components/portfolio/HikageScene.tsx`: 作品本体の描画ロジックを実装するため
- `app/globals.css`: 背景、月、星、十字星の見た目を定義するため

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`

---

## 追加対応: ポートフォリオ個別ページ

### 対応する仕様

- ユーザー依頼: ポートフォリオ作品をモーダルに加えて個別ページでも閲覧できるようにする
- ユーザー依頼: モーダル詳細の右下に「ブラウザで見る」ボタンを追加する
- ユーザー依頼: 個別ページではタイトル、HTML または画像、詳細の順に表示し、詳細は常時表示にする

### 実装方針

- 作品ごとの個別ページは `app/portfolio/[id]/page.tsx` を追加し、既存の `portfolioItems` を元に静的生成する
- モーダル表示の既存動線は維持し、詳細欄の右下に個別ページへの遷移ボタンだけを追加する
- 個別ページ側ではモーダルのアコーディオンを使い回さず、作品情報を常時表示する専用レイアウトにする
- 画像作品と HTML 作品の両方を既存の `PortfolioMedia` で再利用し、見た目の差異はページ用スタイルで吸収する

### 変更予定のファイルと理由

- `data/portfolio.ts`: 個別ページルートに使う URL を作品 ID ベースへ正規化するため
- `components/ui/PortfolioGallery.tsx`: モーダル詳細内に「ブラウザで見る」導線を追加するため
- `app/portfolio/[id]/page.tsx`: ポートフォリオ個別ページ本体を実装するため
- `app/globals.css`: 新しい導線ボタンと個別ページ表示用スタイルを追加するため

### 影響範囲

- `/portfolio` のギャラリー一覧とモーダル詳細 UI
- `/portfolio/[id]` の新規静的ページ
- ポートフォリオ作品データの内部リンク先

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`

### 懸念点・制約・未確定事項

- 既存データの `href` は一覧トップを指しているため、作品 ID ごとの個別ページへ揃える必要がある
- HTML 作品はサムネイルと本体表示でサイズ制御が異なるため、個別ページでの横幅制御を CSS で確認する

---

## 追加対応: ポートフォリオ HTML 作品「猿でもシェイクスピア」

### 対応する仕様

- ユーザー依頼: `public/portfolio/saru-demo/saru.png` をサムネイルに使い、`saru-demo` の HTML 作品を追加する
- ユーザー依頼: レベル切り替え、文字固定、シェイク、クリア判定、クリアモーダルを持つゲームを実装する
- ユーザー依頼: 一番下に `Xに投稿` ボタンを置き、スクリーンショット投稿可否の実装条件を事前に整理する

### 実装方針

- `portfolio` 側の HTML 作品として追加し、既存の HTML 作品レンダラーに新しい専用コンポーネントを登録する
- ゲーム状態はクライアントコンポーネント内で完結させ、レベルごとの目標語、固定可能数、UI文言を定数で管理する
- 文字はカタカナ 1 文字ずつのボックスとして表示し、固定可能なレベルのみクリックで固定 / 解除できるようにする
- `シェイク` 時は固定されていない文字だけをランダム再抽選し、完成時には小さなモーダルを表示する
- `Xに投稿` は直接の画像添付投稿がクライアント単体では困難なため、今回は制約を明示するボタン挙動に留める

### 変更予定のファイルと理由

- `data/portfolio.ts`: 新しい HTML 作品データを登録するため
- `components/portfolio/PortfolioHtmlComponents.tsx`: 新しい HTML 作品レンダラーを登録するため
- `components/portfolio/SaruDemoScene.tsx`: ゲーム本体を実装するため
- `app/globals.css`: ゲーム用レイアウト、文字ボックス、モーダル、案内ボタンの見た目を追加するため
- `plan.md`: 今回の実装方針と制約を記録するため

### 影響範囲

- `/portfolio` 一覧のカード、モーダル、個別ページ
- HTML 作品レンダラーの型と分岐
- モバイル時の HTML 作品表示高さとボタン配置

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`

---

## 追加対応: Portfolio カテゴリ統合

### 対応する仕様

- ユーザー依頼: `Games` ページとメニューはなくし、`Portfolio` にイラスト・HTMLアート・ゲームのカテゴリを持たせる
- ユーザー依頼: カテゴリは `kind` とは別管理にし、`Portfolio` 一覧で絞り込みできるようにする
- ユーザー依頼: `saru-demo` は `Portfolio` 側に残し、既存リンクを維持する

### 実装方針

- `portfolioItems` に表示用カテゴリを追加し、`kind` は描画方式の判定専用として分離する
- `PortfolioGallery` にカテゴリフィルター UI を追加し、一覧のカード表示だけを切り替える
- `Games` のメニュー・専用データ・専用ページは削除し、`saru-demo` は `/portfolio/saru-demo` で引き続き表示する

### 変更予定のファイルと理由

- `data/portfolio.ts`: カテゴリ定義と各作品のカテゴリ設定を追加するため
- `components/ui/PortfolioGallery.tsx`: カテゴリ絞り込み UI を追加するため
- `components/layout/Sidebar.tsx`: `Games` メニューを削除するため
- `app/globals.css`: カテゴリフィルターの見た目を追加するため
- `app/games/page.tsx`, `app/games/[id]/page.tsx`, `data/games.ts`: 不要になったため削除するため

### 影響範囲

- サイドバーのメニュー表示
- `/portfolio` 一覧の絞り込み UI
- `/games` ルートの削除

### 検証方法

- `npm run lint`
- `npm run build`

---

## 追加対応: What's New 自動差し込み

### 対応する仕様

- ユーザー依頼: What's New に手動ニュースだけでなく、公開日付きの Works / Portfolio 追加情報を自動表示する
- ユーザー確認: `date` が設定されているもののみ表示し、同日では手動ニュースを優先する
- ユーザー確認: 自動追加分はニュース一覧で「その他」として表示し、個別ニュースページは作らず、リンク先は成果物またはポートフォリオ本体にする

### 実装方針

- `data/news.ts` に手動ニュース配列と画面表示用の合成ニュース配列を分けて定義する
- Works / Portfolio から `date` がある項目だけを「その他」カテゴリの自動ニュースとして生成する
- 表示用配列は日付降順にし、同じ日付では手動ニュース、Works、Portfolio の順に並べる
- ニュース個別ページの生成・取得は手動ニュースのみを対象にする

### 変更予定のファイルと理由

- `data/works.ts`: Works に公開日を設定できる型を追加するため
- `data/news.ts`: 手動ニュースと自動ニュースの合成処理を追加するため
- `components/layout/Sidebar.tsx`: What's New のリンク先を記事種別に応じて切り替えるため
- `components/ui/NewsCard.tsx`: ニュース一覧カードのリンク先を記事種別に応じて切り替えるため
- `app/news/page.tsx`: 合成ニュース配列で一覧とカテゴリフィルターを表示するため
- `app/news/[id]/page.tsx`: 個別ページ対象を手動ニュースのみに限定するため
- `SPEC.md`: 新しいニュース表示仕様と Works 公開日を正式化するため

### 影響範囲

- サイドバーの What's New 表示
- `/news` 一覧とカテゴリフィルター
- `/news/[id]` の静的生成、SEO、構造化データ
- Works / Portfolio のデータ追加運用

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`

---

## 追加対応: Portfolio HTMLアート `kokoro`

### 対応する仕様

- ユーザー依頼: `public/portfolio/kokoro` の素材を使って HTMLアート作品を `Portfolio` に追加する
- ユーザー依頼: `base.png` の上に `eye1.png`、下に `wing.png`、`bg.png` の順で重ねる
- ユーザー依頼: 背景は朝・昼は白、夜・深夜は `rgb(187,184,230)` とし、時間判定は既存テーマと同じにする
- ユーザー依頼: `wing` の位置は手動調整しやすくする

### 実装方針

- `data/portfolio.ts` に HTMLアート作品として `kokoro` を追加する
- `components/portfolio/` に専用レンダラーを追加し、`useTimeTheme` の `theme` を参照して背景色を切り替える
- `base`、`eye1`、`bg` は同寸法なので、同じフレームへ絶対配置してぴったり重ねる
- `wing` は別寸法のため、相対位置・相対幅の定数で管理して手調整しやすくする

### 変更予定のファイルと理由

- `data/portfolio.ts`: 作品データとレンダラー ID を追加するため
- `components/portfolio/PortfolioHtmlComponents.tsx`: 新しい HTMLアートレンダラーを登録するため
- `components/portfolio/KokoroScene.tsx`: 作品本体を実装するため
- `app/globals.css`: `kokoro` 用レイアウトと重ね順のスタイルを追加するため

### 検証方法

- `npm run lint`
- `npm run build`

---

## 追加対応: Works ツール開発「アクキー確認ツール」

### 対応する仕様

- ユーザー依頼: Works 内に、アクキーの見た目を確認できる専用ページを追加する
- ユーザー依頼: 画面上へ PNG をドラッグし、透過部分の周りへ透明領域を作り、その外側に白いハイライトを付ける
- ユーザー依頼: イラストの非透過部分だけに `mix-blend-mode: multiply` 相当の暗さをかけ、透明領域・ハイライト・背景へ色を載せない

### 実装方針

- `works` のツール開発カテゴリに新規項目を追加し、既存の `/works/[id]` 詳細導線を使う
- 詳細ページでは対象 ID のみクライアントコンポーネントを差し込み、PNG ドラッグ&ドロップとファイル選択を受け付ける
- Canvas で PNG のアルファマスクを抽出し、マスク膨張でアクリル透明領域と白い外周ハイライトを描画する
- 元画像はアルファ部分だけ別 Canvas に描き、CSS の `mix-blend-mode: multiply` と透明度で背景に馴染ませる

### 変更予定のファイルと理由

- `data/works.ts`: ツール開発カテゴリの成果物として追加するため
- `app/works/[id]/page.tsx`: 対象詳細ページに専用ツールを表示するため
- `components/works/AcrylicKeychainTool.tsx`: PNG 読み込みとアクキー見た目生成を実装するため
- `app/globals.css`: ツール画面、ドラッグ領域、プレビュー表示を既存デザインに合わせるため
- `public/works/acrylic-keychain-tool.svg`: Works 一覧用のサムネイルを追加するため

### 影響範囲

- `/works` 一覧、カテゴリ絞り込み
- `/works/acrylic-keychain-tool` 詳細表示
- クライアント側 Canvas / File API を使うブラウザ表示

### 検証方法

- `npm run lint`

## 追加対応: アクキー確認ツール SVG/API 書き出し

### 対応する仕様

- ユーザー依頼: アクキー作成処理はHTML/ブラウザ側のまま維持する
- ユーザー依頼: SVG作成処理だけサーバーAPIにする
- ユーザー依頼: アクキー作成後に「このまま発注する」ボタンを追加する
- ユーザー依頼: 既存のアクキー作成処理で作ったアウトラインを再利用し、処理が二重にならないようにする
- ユーザー依頼: 当初はPNGとSVGをzipにまとめる想定だったが、一旦zip出力を停止してSVG単体にする
- ユーザー依頼: まずは業者連携前にSVG化できることを証明し、`npm run dev`でAPIをテスト可能にする
- ユーザー依頼: SVGだけを出力し、制御点を赤く表示する

### 実装方針

- 既存のCanvasプレビュー生成処理は維持し、マスク生成時にSVG用カットラインの境界点列を`PreviewState`へ保持する
- 「SVGを書き出す」ボタン押下時は、配置済みイラストPNGをAPIへ送信する
- `app/api/acrylic/export/route.ts`を追加し、受け取ったPNGと境界点列からサーバー側で点を整えてSVG文字列を作成する
- 二重の丸以外のアウトラインは、境界点から外向き角をQ曲線化する基本的な仕組みに戻す
- 穴ありの場合は境界点列から穴を再現せず、HTML側で決定した穴の中心・半径をAPIへ渡し、SVG側で正円パスとして出力する
- 内側の穴は上下左右4点を基準にCベジェで正円化し、外側の穴まわり上半分はHTML側で決定した外側円形データから3点相当のなだらかな曲線として出力する
- API側でSVG単体を返し、Q/C曲線の制御点を赤い点としてSVG内に描画する
- 制御点間の直線がSVG枠へ収めたサイズ比例のしきい値を超える場合、その直線だけ青く重ねて表示する
- ほぼ並行な長い直線が連続する場合は青線表示を両端へまとめ、時計回りに見た直線間の非直線部分に該当する制御点だけを一つおきに緑で表示する
- 直線同士が隣接する箇所は緑判定を行わず、直線を構成する端点も緑表示から除外する。緑は赤と同じ制御点の色違いとして表示する
- 業者別の形式調整や外部API連携は今回の範囲外とし、SVG化の証明に必要な最小構成に留める

### 変更予定のファイルと理由

- `components/works/AcrylicKeychainTool.tsx`: SVG用API呼び出しとSVGダウンロードボタンを追加するため
- `app/api/acrylic/export/route.ts`: SVG生成とSVG単体レスポンスを行うサーバーAPIを追加するため
- `app/globals.css`: 追加ボタンと状態表示を既存UIに合わせるため
- `plan.md`: API追加を含む複数ファイル変更の方針を記録するため

### 影響範囲

- `/works/acrylic-keychain-tool` のクライアント側ツール操作
- `/api/acrylic/export` のローカルdev/Vercel PreviewでのPOST API
- ブラウザのBlob APIを使ったSVGダウンロード

### 検証方法

- `npm run lint`
- `npm run build`
- `npm run dev`でAPIが起動できることを確認する
- `npx tsc --noEmit`
- `npm run build`

## 追加対応: SVG生成をサーバー側画像解析方式へ変更

### 対応する仕様

- ユーザー依頼: イラストをサーバーへ送り、SVG生成はサーバー側で行う
- ユーザー依頼: HTMLと同じ方式で二重の丸の位置を決め、下部を下に伸ばす処理も維持する
- ユーザー依頼: イラスト外周を膨らませてアウトラインを作成する
- ユーザー依頼: 内側を向いた直角より鋭い角はそのまま残し、外側を向いた角はなだらかな曲線に変換する

### 実装方針

- クライアントは発注時に合成PNGと、配置済みイラストPNG、穴あり/なしモードをAPIへ送信する
- APIは配置済みイラストPNGのアルファを読み取り、サーバー側でベースマスク、穴位置、下部接続、クリアランス膨張マスクを再作成する
- SVGパスはサーバー生成マスクから境界を取り、二重円以外の外周は基本的なQ曲線の制御点付きアウトラインとして出力する
- 境界点を制御点として表示し、複雑なギザギザ削除や区間解析は行わず、アウトラインの元になる点を確認できる状態にする
- 直線部分と二重円部分は除外し、外周の制御点のうち前後3点の関係で内側へ折れている点だけを緑で表示する。長い直線の両端に位置する制御点は緑表示から除外する
- 黄色表示を戻し、長い直線同士が端点で接続または短いピンク区間を挟んで近接し、ほぼ同じ向きで連続する黄色グループを `opIndex` つきで保持する。その同じ黄色グループ範囲だけを、実際のアウトラインパス上でも端から端まで1本の直線にまとめる
- 青線または黄色線として判定された直線に `Q` 曲線が隣接する場合、短いピンク直線を1本挟んで `Q` 曲線が続く場合、または赤い `Q` 制御点自体が青/黄色直線の端点に近い場合だけ、その `Q` の制御点と手前の短いピンク線を削除する。青/黄色直線の元の長さは変えず、削除後の次の命令開始点だけを元の直線端に合わせる
- 実SVGでは直線端のあとに `L→L→Q` と短い直線が複数挟まるケース、または `Q` の制御点ではなく終点側が横に逃げるケースがあるため、直線端から近い `Q` は制御点方向と終点方向の両方を見て、不要な制御点として削除する
- 直線端からごく短く、直線方向とは違う方向へ逃げる短い `L` 直線も、次の点が近い場合だけ不要な折れとして削除し、青/黄色直線から次の点へ直接つなぐ
- `REMOVE_INNER_CONTROL_POINTS` が有効な場合のみ、くぼみ判定で緑になった制御点そのものを点列から削除し、残った点を使って後続のパス生成を行う
- 制御点マーカーは `detectConcavities(...)` の検出結果を `Set` 化し、`Q` 曲線の制御点だけでなく元の `pathPoints` 上の角点も表示する。くぼみとして判定された鋭角の中心点だけを緑、それ以外を赤で表示する。くぼみ判定は内向きの角のうち、前後点が作る角度が `CONCAVITY_MAX_ANGLE` 以下の鋭角に限定する。緑線や補助線は追加せず、点の色分岐だけでくぼみ検出を確認する
- 二重円以外の外周は、緑のくぼみ中心点が2点以上ある場合、緑点をアンカーとして区間分割する。各区間内の `pathPoints` は通過点ではなくふくらみ方向と量を測るサンプルとして扱い、緑点から緑点までを同じ側に制御点を置いた三次ベジェ `C` でつなぐ。ふくらみ方向が途中で反転する場合だけ少数区間に分割する
- 緑点間の三次ベジェ近似は、生成した曲線と区間内の赤点との最短距離をサンプリングで測る。最大誤差が `BASE_CURVE_FIT_MAX_ERROR` を超える場合は、最も離れた赤点で区間を2分割し、上限回数まで再帰的に再フィットする
- 再フィットで分割された三次ベジェ同士は、共有点の左右ハンドルを平均接線方向へ揃え、分割点が尖って見えないようにする
- 比較確認用に `ENABLE_STRAIGHT_LINE_PROCESSING` を `false` にすると、直線候補の結合、直線端付近の制御点削除、RDP簡略化、滑らか区間の `L` 変換を無効化し、くぼみ関連の処理はそのままにする
- `ENABLE_STRAIGHT_LINE_PROCESSING` が `true` の場合は、隣接する2点間の距離がしきい値以上の線だけを長い直線区間として検出し、その両端をくぼみ判定から保護する。複数点をまたいで両端を結ぶ直線判定は行わない。削除後の点列でも直線区間を再検出し、直線区間の両端をスムージングの区切り点に追加して、直線が曲線へ吸収されないようにする
- 緑の制御点表示は、直線保護用の `protectedPointKeys` を渡さないくぼみ判定結果を使う。これにより、削除対象から保護された直線端点でも、形状としてくぼみなら緑点として確認できるようにする
- `SHOW_LONG_STRAIGHT_LINE_MARKERS` が `true` なら連続していない長い直線区間を青で表示し、`SHOW_CONNECTED_STRAIGHT_LINE_MARKERS` が `true` なら直線区間同士がピンクのパスを挟まず連続している区間を黄色の `connectedStraightLines` として表示する。連続直線は青側には含めず、表示上の分類のみで実際のパス生成は変更しない
- 長い直線区間を青/黄色候補にする前に、その両端から次の緑くぼみ点までの点列方向を確認する。両側とも直線方向から `STRAIGHT_TO_CONCAVITY_STEEP_MIN_ANGLE` 以上急に曲がる場合、または直線端点自体が緑点の場合は直線候補を残し、どちらか片側でも緩い場合は直線候補から外す
- 内側点削除は、元の `pathPoints` に対して先に `detectConcavities(...)` を実行し、緑点として判定されたくぼみ中心点だけを削除する。短辺条件による別ロジック削除や反復削除は行わず、表示上の緑点と実際の削除対象を一致させる
- 処理順は、元の `pathPoints` に対するくぼみ判定、緑点だけの削除、削除後 `pathPoints` に対する再くぼみ判定、緑点アンカーを使ったスムージングの順にする。くぼみ判定自体では赤点を追加削除しない
- `DEBUG_OUTPUT_PRE_SMOOTHING_PATH` を `true` にすると、緑点削除と削除後くぼみ判定の直後、スムージング前の `pathPoints` をそのまま線で結んだ確認用SVGを出す
- `DEBUG_OUTPUT_RAW_BASE_MASK_PATH` を `true` にすると、隙間埋め・穴追加後、`clearRadius` による最終アクリル余白の膨張前の `keychainMask` をそのまま外周ベクター化して返す。不要なアウトラインが発生する段階を切り分けるための確認用フラグとする
- `DEBUG_OUTPUT_HOLE_FILL_TARGET_MASK_PATH` を `true` にすると、`ENABLE_NARROW_EXIT_AREA_FILL` が実際に `filledBaseMask` へ追加する膨張済みパッチ領域だけを青く塗りつぶした確認用SVGとして返す。`false` の場合は青表示へ切り替えず、既存の最終カットパス生成へ進む
- SVG出力APIでは `fillNarrowTransparentGaps` を使わず、通常経路では `filledBaseMask` をそのまま次工程へ渡す。`DEBUG_OUTPUT_NARROW_EXIT_AREA_MASK_PATH` と `DEBUG_OUTPUT_PRE_SMOOTHING_PATH` が `true` の場合は、スムージング前の確認用SVGに、`filledBaseMask` を膨張して狭い出口を仮に閉じたあと、穴埋めで `dilatedMask` から追加された大きな閉領域だけを青い補助パスとして重ねる
- `ENABLE_NARROW_EXIT_AREA_FILL` を `true` にすると、青判定した閉領域をさらに膨張し、膨張後穴埋めマスク内に収まる範囲だけ `filledBaseMask` へ合成して、既存のパス生成を維持したまま以降のキーホール追加・最終余白生成に渡す
- HTMLプレビュー側は軽さを優先し、従来の `fillNarrowTransparentGaps` を使用する
- HTMLプレビュー側で小さな穴埋め残りが出る場合に備え、`fillNarrowTransparentGaps` の判定半径だけ `HTML_PREVIEW_GAP_CLOSE_RADIUS_MULTIPLIER` で広げる。複雑な領域判定は追加しない
- HTMLプレビューの背面色レイヤーは、元の `baseMask` ではなく穴埋め済みの `gapClosedBaseMask` を使い、穴埋め後に広がった領域が白く抜けて見えないようにする
- SVG出力APIは `debug` フラグで返却形式を切り替える。`debug: true` は現状の確認用SVGを返し、`debug: false` は背景や制御点を含まないピンクのカットパスだけのSVGと元PNGをZIPにまとめて返す
- 最終出力の目視確認では制御点マーカーを非表示にし、アウトラインの線そのものだけを確認できる状態にする
- 二重円の上半分と内側穴は境界追跡に頼らず、計算した中心・半径から正円として出力する
- アウトライン確認用に、SVG内へ元イラストPNGを同じサイズで最背面に埋め込み、カットラインとの差を見られるようにする

---

## 追加対応: Portfolio HTMLアート `maid`

### 対応する仕様

- ユーザー依頼: `public/portfolio/maid/base.png` をベースに HTML作品を作る
- ユーザー依頼: `base.png` の裏へ同じ表示サイズで `eye-highlight.png`、`eye.png`、`eye-hole.png` の順に重ねる
- ユーザー依頼: 一番奥の背景は常に白にする
- ユーザー依頼: 画像読み込みが重い時に、軽いcanvasだけが先に描画されないようにする
- ユーザー依頼: 背景の一番奥に `rgb(206,206,206)` の小さなドットを斜め配置で敷き詰め、スマホでも右下へ進み続けて見えるループにする
- ユーザー依頼: `wind.png` を背景ドットの上に表示し、`kokoro` の `wing.png` と同様に右支点でゆっくり動かし、クリック/ドラッグで動かせるようにする
- ユーザー依頼: `star.png` を3つ別々に回転配置し、すべて同じ速さでゆっくり右回転させる。配置場所と大きさは個別に手動調整できるようにする
- ユーザー依頼: PC版のみ、右上の星クリック後にこのページ内のマウスカーソルを `star.png` 由来の見た目へ変更し、星ホバー時は手ポインターにしない
- ユーザー依頼: 羽を触っている間は目の横方向移動を禁止し、ドラッグ中のみ目を上下させ、反発中は離した時点の目位置を保持する
- ユーザー依頼: 羽を離してから目線が戻るまでの間隔を長めにし、目の追従ラグはなくす
- ユーザー依頼: キャンバス内でマウス移動またはタッチ開始した時、`eye.png` を素早くその方向へ移動し、キャンバス外ではしばらくその位置で静止させた後、スクロール中心とキャンバス位置に応じてゆるやかに上下へ動かす
- ユーザー依頼: スクロール中心による外側視線で下を見る場合のみ、下方向の上限を元画像px相当で `2` 程度に収める
- 実ファイル確認: `public/portfolio/maid/wind.png` は存在せず、`public/portfolio/maid/wing.png` が存在するため、現時点では `wing.png` を風レイヤーとして使用する

### 実装方針

- `data/portfolio.ts` に HTMLアート作品として `maid` を追加する
- `components/portfolio/` に専用レンダラーを追加し、全レイヤーを同じ `669 / 1000` のステージへ絶対配置する
- 必要なPNGレイヤーの読み込み完了を数え、準備ができるまでは白背景だけを表示してcanvasや画像レイヤーを隠す
- 最背面の疑似要素に繰り返しドット背景を敷き、スマホでも動くように `requestAnimationFrame` でCSS変数を更新して疑似要素を右下へ移動させる
- ドットの大きさと間隔はコンテナ幅基準の `cqw` で指定し、個別ページは個別ページの表示サイズのまま、作品サイズに対するドット比率をモーダルと揃える
- 風レイヤーは背景ドットより前、人物レイヤーより後ろへ置き、右側を `transform-origin` にして `requestAnimationFrame` による常時スロー揺れとドラッグ後の戻りアニメーションを実装する
- 風レイヤー位置とクリックエリアは `MaidScene.tsx` の定数で調整できるようにし、クリックエリアは初期確認用に赤表示する
- 星は `MaidScene.tsx` の `STAR_ITEMS` で位置・サイズ・初期角度を個別指定し、CSS keyframesで同じ速度の右回転にする
- 右上の星だけ透明ボタンを重ね、PCのfine pointer環境ではクリック時に `star.png` を小さなカーソル用data URLへ変換してページ全体に適用し、コンポーネントのアンマウント時に解除する
- 羽クリック時は現在の目線Yを保持したままXだけ `0` に固定し、ドラッグ中は通常の `EYE_FOLLOW_RANGE` と同じY制御で目を動かす
- 羽ドラッグ終了処理は `pointerup` と `lostpointercapture` の二重実行で反発状態を消さないように、ドラッグ中のみ実行する
- 羽の反発中は目の追従アニメーションを行わず、離した時点の目位置を保持する
- 羽を離してから目線を戻すまでは通常のポインター追従とキャンバス外リセットをロックし、`WIND_RESUME_DELAY_MS` が目線保持時間として効くようにする
- 羽を離した後の目線リセットは `WIND_RESUME_DELAY_MS` で間隔を調整し、目の `transform` には transition を付けない
- `eye.png` はフレーム中央からポインター位置への差分をもとに移動し、移動範囲は `MaidScene.tsx` の `EYE_FOLLOW_RANGE` で元画像px相当として調整する。キャンバス外では一定時間保持後、ビューポート中心がキャンバスより上なら上、下なら下を見るように遷移させ、下方向だけ `EYE_OUTSIDE_FOLLOW_RANGE.bottom` で小さく抑える

### 変更予定のファイルと理由

- `data/portfolio.ts`: 作品データとレンダラー ID を追加するため
- `components/portfolio/PortfolioHtmlComponents.tsx`: 新しい HTMLアートレンダラーを登録するため
- `components/portfolio/MaidScene.tsx`: 作品本体を実装するため
- `app/globals.css`: `maid` 用レイアウトと重ね順のスタイルを追加するため

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`

---

## 追加対応: 募金ページの仮設置

### 対応する仕様

- ユーザー依頼: `public/bokin/header.png` を使って `bokin` ページを作成する
- ユーザー依頼: ページ上部に `header.png` を設定し、指定文言と「50円から支援する」ボタンを表示する
- ユーザー依頼: Menu の下に `public/bokin/header.png` を使った案内バナーを配置し、募金ページへ遷移させる
- ユーザー依頼: PC版の案内バナーは Message の上に配置する
- ユーザー依頼: `/bokin` ページでは案内バナーを表示しない
- ユーザー依頼: Stripe連携テスト用にCheckout作成APIを追加し、決済後は専用テスト画面へ移動する
- ユーザー依頼: 専用画面は決済後以外ではアクセスできないようにする
- ユーザー依頼: Stripeから戻る操作で404にならないようにし、支援額は50円以上を1円単位で任意入力できるようにする
- ユーザー依頼: 募金案内に「支援金の10%は熊本震災の募金に使用する」旨と対象期間、外部寄付URLを追加する
- ユーザー依頼: 支援金額に「+100」「+1,000」「+10,000」の丸枠ボタンを追加し、手入力欄も残す
- ユーザー依頼: 支援するボタン押下後にモーダルで任意の表示名入力欄を出し、未入力時は匿名希望として扱う旨を記載する
- ユーザー依頼: 支援額に応じて名前付きの支援メッセージをMessage欄へ表示し、50〜499円は青、500〜2999円は黄色、3000〜9999円は赤、10000円以上は虹色にする
- ユーザー依頼: `public/bokin/tensi.png` と `face1〜5.png` を募金ページ右下に重ねて配置し、支援額に応じて表情を切り替える

### 実装方針

- 新規ページ `app/bokin/page.tsx` を既存の `SiteFrame` と `RetroPanel` で作成する
- `header.png` は 4:1 画像として、ページ本文上部とサイドバー案内バナーで共用する
- サイドバー案内は既存ナビ項目には追加せず、PCでは Message 直上、スマホでは What's New 直上の独立したリンクバナーにする
- `POST /api/bokin/checkout` でStripe Checkout Sessionを作成し、成功時は `/bokin/thanks?session_id={CHECKOUT_SESSION_ID}` へ戻す
- `/bokin/thanks` は `session_id` からStripe Sessionを取得し、募金用メタデータ、`paid`、`complete` を満たさない場合は `/bokin` へ戻す
- Checkoutの戻り先URLはリクエスト元のhost/protocolを優先し、ローカルテスト中に本番URLへ戻らないようにする
- 支援額は `type="number"` の入力欄で50円以上・1円単位とし、API側でも同条件を検証する
- 金額入力フォームはクライアントコンポーネント化し、加算ボタンで現在値へ指定額を加えられるようにする
- 表示名は支援ボタン押下後のモーダルで任意入力、最大20文字、未入力時は `匿名希望` としてStripe metadataへ保存する
- `REDIS_KEY_PREFIX=dev` の場合はアクセスカウンターとキリ番キーにも `dev:` を付け、本番の `site:counter:*` へ書き込まない
- 決済後ページでpaid/complete確認後、セッションIDごとに一度だけRedis listへ支援メッセージを保存する
- Messageパネルは `/api/messages` からRedis上の支援メッセージを取得し、既存の静的メッセージと時刻順に統合して表示する
- 募金ページのキャラクターは画像レイヤーを同サイズで重ね、全レイヤー読み込み完了まで非表示にして、`tensi.png` 単体表示を防ぐ
- キャラクター全体のサイズと右下位置はCSS変数で調整可能にし、顔レイヤーは本体と同一サイズ・同一位置で固定する

### 変更予定のファイルと理由

- `app/bokin/page.tsx`: 募金ページを追加するため
- `components/ui/BokinSupportForm.tsx`: 支援金額の加算ボタンと手入力欄を管理するため
- `public/bokin/tensi.png`, `public/bokin/face1.png`〜`face5.png`: 募金ページキャラクター表示に使用するため
- `app/bokin/thanks/page.tsx`: 決済後専用のテスト完了画面を追加するため
- `app/api/bokin/checkout/route.ts`: Stripe Checkout Sessionを作成するため
- `app/api/messages/route.ts`: MessageパネルへRedis上の支援メッセージを返すため
- `components/layout/Sidebar.tsx`: Menu 下に募金ページへの案内バナーを配置するため
- `components/ui/MessagePanel.tsx`: 支援メッセージを動的取得して色分け表示するため
- `app/globals.css`: 募金ページ画像とサイドバー案内バナーの表示を整えるため
- `lib/counter.ts`: 開発用Redis prefixで本番カウンターへの書き込みを避けるため
- `lib/redis.ts`: Redis接続とprefix付きキー生成を共通化するため
- `lib/bokinMessages.ts`: 支援メッセージの保存、取得、色分類を管理するため
- `README.md`: `REDIS_KEY_PREFIX` のローカル運用を説明するため

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`

---

## 追加対応: アクスタプレビュー処理

### 対応する仕様

- ユーザー依頼: アクキーシミュレーターへ「アクキー | アクスタ」トグルを追加する
- ユーザー依頼: アクスタでは上部の穴パーツを追加しない
- ユーザー依頼: アクスタの下側処理は、底面エリアではなく最下点から近い高さの左右点へ水平線と縦線で接続する
- ユーザー依頼: SVG作成はまだ実装しない

### 実装方針

- 既存のPNG解析、マスク拡張、プレビュー描画処理を再利用する
- 商品種別を `keychain` / `stand` として追加し、プレビューキャッシュは種別と形状モード単位で保持する
- アクキーは既存の「穴あり」「穴なし」を維持する
- アクスタは「シンプル」「安定」を既存の穴モード切替と同じ位置に表示する
- 下側処理は最下点の高さを基準に、近い高さと判断する許容差をコード内定数で変更できるようにする
- 近い高さの点はx距離に関係なくyだけで判定し、同方向に複数候補がある場合は一番遠いxを採用する
- 近い高さの点が左、右、または両方にある場合、それぞれ最下点と同じyの水平線を対象xまで伸ばし、そこから縦線で上方向へ接続する
- SVG書き出しはアクキー時のみ表示する

### 変更予定のファイルと理由

- `components/works/AcrylicKeychainTool.tsx`: 商品種別トグル、アクスタ用マスク生成、状態分岐を追加するため
- `app/globals.css`: 商品種別トグルのスタイルを追加するため

### 影響範囲

- アクキーシミュレーターのプレビュー生成、表示切替、SVG書き出しボタン表示
- PNG選択前後の操作UIとステータス表示
- モバイル表示時のトグル配置

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- アクキー/アクスタ、穴あり/穴なし、シンプル/安定の切替とプレビュー再生成を手動確認

### 懸念点・制約・未確定事項

- アクスタの下側アウトライン生成はプレビュー用の近似実装とし、SVG作成処理にはまだ反映しない

---

## 追加対応: アクリルプレビュー変換API分離

### 対応する仕様

- ユーザー依頼: アクキー/アクスタともに表示用レイヤー画像生成までAPI側で行い、回転や見た目の重ね合わせだけHTML側で行う
- ユーザー依頼: 既存のSVG export APIとは別の場所に分けて管理する

### 実装方針

- 新規API Route `/api/acrylic/preview` を追加する
- API側でPNG data URLを解析し、最大サイズ制限、透明領域の切り抜き、マスク生成、穴/アクスタ下側接続、アクリル/エッジ/背面/イラストの表示用PNG生成を行う
- フロント側の `buildPreview` はPNGをdata URL化してAPIへ送信し、返却されたレイヤーを既存の回転プレビューに渡す
- SVG export API `/api/acrylic/export` は既存どおり維持し、プレビューAPIとは分離する
- フロント側に残る処理はファイル選択、API呼び出し、画像レイヤーの拡大縮小描画、回転UI、SVG書き出し呼び出しに限定する

### 変更予定のファイルと理由

- `app/api/acrylic/preview/route.ts`: 表示用レイヤー生成APIを追加するため
- `components/works/AcrylicKeychainTool.tsx`: クライアント側変換処理をAPI呼び出しへ置き換えるため
- `plan.md`: 作業方針を記録するため

### 影響範囲

- アクキー/アクスタのPNG選択後プレビュー生成
- 穴あり/穴なし、シンプル/安定の切替時の再生成
- SVG書き出し時に使う `originalArtworkSrc` の生成元

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- ローカルサーバーで `/api/acrylic/preview` と対象ページの応答確認

### 懸念点・制約・未確定事項

- サーバー側では追加依存を入れず、PNGの基本形式をNode標準機能で処理する
- ブラウザCanvasの合成/ぼかしと完全同一ではなく、表示用レイヤーはサーバー側の近似生成になる

---

## 追加対応: アクスタ固定台座プレビュー

### 対応する仕様

- ユーザー依頼: アクスタ下側に横長楕円の台座を追加する
- ユーザー依頼: 台座は白い上アウトラインとグレーの下アウトラインをy軸にずらし、横から見た高さのある立体物のように見せる
- ユーザー依頼: 台座内部は塗らず透明にする
- ユーザー依頼: 台座はアクスタ本体と違い回転させず固定表示する
- ユーザー依頼: 楕円の横幅はイラスト横幅と同じにし、あとで%で手動調整できるようにする

### 実装方針

- プレビューAPIではアクスタ時の台座位置・サイズ情報を返し、台座の見た目はHTML/CSSの疑似3Dレイヤーで描く
- ユーザー追加依頼により、台座はアクスタ本体のY軸回転には追従させず、床平面に設置された正円の投影として常に同じ形状を保つ
- 台座幅は `BASE_STAND_BASE_WIDTH_RATIO_PERCENT` でイラスト横幅比として調整可能にする
- アクスタ時は台座表示分の下余白をAPI出力キャンバスに追加し、イラスト本体と台座が同じ座標系に収まるようにする
- 台座の厚み中央がアクスタの透明アウトライン底面に接するよう、台座の高さ方向の配置基準を調整する
- APIから透明アウトライン最下部の接地点 `contactY` を返し、CSS台座上面の中心Yをその接地点に直接合わせる
- フロント側では描画済みアクリルレイヤーのアルファ最下端も測定し、CSS台座上面の中心Yを実表示上のアウトライン最下部に合わせる
- 台座は正円の中心Yがアクスタ底面に合うようにし、不要なグレー塗り面は作らず、下面グレー線と側面ハイライト線だけを描く
- 台座の縦幅は `BASE_STAND_BASE_HEIGHT_RATIO_PERCENT` でイラスト横幅比として調整可能にする
- API側の乗算はイラストレイヤーにのみ適用し、透明・白枠・グレー枠・台座レイヤーには適用しない
- API側の `acrylicSrc` / `edgeSrc` は元HTML処理と同じく、外周透明リング、ずらした白線、ずらしたグレー線の責務で分ける
- フロント側の台座は `acrylic-preview-object` の外側に置き、アクスタ本体の回転で円形状が変わらない床面レイヤーとして扱う
- 台座は上面・側面・下面の楕円レイヤーで構成し、中心Yを透明アウトライン込みの本体最下部へ合わせる
- 台座のグレー領域は左右の縦線間の矩形ではなく、上面下カーブと下面下カーブに挟まれた下側U字パスとして描く
- 台座の厚みは `BASE_STAND_BASE_DEPTH_OFFSET_RATIO` と `STAND_BASE_SVG_THICKNESS_Y` で調整可能にし、下面楕円は上面楕円を同じ形のままY方向へ平行移動して描く
- 台座左右の白線はカーブではなく、上面端と下面端を結ぶ直線として描く
- SVG表示高さは上面楕円の縦幅 `STAND_BASE_SVG_TOP_HEIGHT` だけから逆算し、U字厚み変更で楕円全体の角度や比率が変わらないようにする
- 台座の楕円パスは手書きベジェではなく、正円点列を `STAND_BASE_SVG_TILT_DEGREES` と `STAND_BASE_SVG_PERSPECTIVE` で3D投影して生成する
- 生成点数は `STAND_BASE_SVG_SAMPLE_COUNT` で調整可能にし、重くなりすぎないよう低解像度の点列で固定パス化する
- 台座横面のハイライトは `STAND_BASE_SIDE_HIGHLIGHT_X` / `STAND_BASE_SIDE_HIGHLIGHT_WIDTH` で範囲調整できる縦帯グラデーションとして描く
- 比較用として、既存台座処理は残したまま `acrylic-preview-object` 内にイラスト平面へ垂直な正円台座を追加し、本体と同じY軸回転を受けるようにする
- 比較中は `SHOW_STAND_BASE_SVG = false` で元の台座SVGを非表示にし、試作用正円だけを表示する
- 試作用正円は `rotateX(90deg)` でイラスト面に対して垂直に立てる
- 試作用正円は上側を白枠、奥側を `--stand-circle-depth` 分だけずらしたグレー枠として厚みを出す
- 試作用正円の側面は2D SVGではなく、円周に短い3Dパネルを並べた低ポリゴンの短い円柱として作り、上面白ラインと下面グレーラインの間の側面だけを半透明グレーで塗る
- 試作用正円の上側白枠は太めの白単色にし、影やグレーラインは使わない
- 試作用正円の正面側太枠は固定の半円ではなく、`rotation.y` に合わせて白い太枠マスクを回し、どの角度でも画面手前側だけが太く見えるようにする
- 試作用正円の厚みはアクスタ本体の白面 `translateZ(6px)` とグレー線 `translateZ(-4px)` の差と同じ `STAND_CIRCLE_DEPTH_PX = 10` に合わせる
- 試作用正円の奥側ラインと側面影は本体画像の後ろ、手前側ラインだけ本体画像の前に分離し、イラストを奥側の線や影が貫通して見えないようにする
- 試作用正円は読み込み直後から見えるよう、画像data URL生成完了条件から独立してマウントし、本体画像レイヤーより前面の `z-index: 5` で表示する
- 試作用正円の位置CSS変数はAPIの `standBaseFrame` から先に設定し、canvas実測値が取れたあと必要に応じて更新する
- 初回正面時だけ台座が消える症状は、角度変更後に表示され続ける挙動からCSS 3Dの初回合成/カリング問題として扱い、台座レイヤーに `will-change` と `translate3d(0, 0, 0)` を付与して初回から合成対象にする
- 映像確認後、初回だけ本体表示より台座style設定が遅れる可能性を潰すため、`preview.standBaseFrame` から台座CSS変数をrender中に同期計算する
- シンプルタイプのアクスタには、イラスト中心から接地面左右端までの短い方を基準にした左右対称のツメ位置を作る
- ツメ位置は接地面端から `BASE_STAND_CLAW_EDGE_GAP` 分だけ内側へ寄せ、接地面を飛び出さないようにする
- ツメは接地面Yから3D台座の側面帯 `standBaseDepthOffset` と同じ長さだけ下へ伸ばし、指定幅・指定丸角のタブ形状として透明アウトラインへ合成する
- 台座位置はツメ先ではなく、ツメ追加前の接地面を基準にしたまま固定し、ツメが台座内へ食い込む見た目にする
- ツメは1pxのU字線を太らせる方式ではなく、上は接続用の直線、左右はまっすぐ下がり、下端だけ小さく丸めたタブ形状で作る
- 接地面ラインのY位置は生マスクの接地Yではなく、透明アウトライン膨張後の底面ラインに合わせて `clearRadius` 分だけ下へ補正する
- ツメ本体は台座側面帯と同じ `rgba(116,122,138,0.16)` 相当の半透明グレーを、グレーアウトラインと同じZ軸の `edgeSrc` へ直接合成する
- ツメ本体のグレー塗りは矩形範囲ではなく、ツメあり `clearMask` とツメなし `clearMaskWithoutClaws` の差分から作った最終アウトライン差分マスクに沿って行う
- ツメグレーは `backSrc` へ敷くのではなく、表裏共通で表示されるグレーアウトラインレイヤーに置く
- ツメの最終幅・角丸はキャラ余白に引っ張られないよう、アクスタ本体の外周余白マスクとは分けて各レイヤーへ合成する
- 平面プレビューではAPIが返す実際のアウトライン・アクリルレイヤーを主表示しつつ、再生成前のスライダー値に追従するツメ幅ガイドを重ねる

### 変更予定のファイルと理由

- `app/api/acrylic/preview/route.ts`: 台座レイヤー生成とアクスタ用キャンバスサイズ調整を追加するため
- `components/works/AcrylicKeychainTool.tsx`: 固定台座レイヤーを描画・表示するため
- `app/globals.css`: 固定台座レイヤーの重なり順を設定するため
- `plan.md`: 作業方針を記録するため

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- `/api/acrylic/preview` へアクキー/アクスタをPOSTし、アクスタだけ `standBaseFrame` が返ることを確認

---

## 追加対応: アクスタ安定底面

### 対応する仕様

- ユーザー依頼: アクキーシミュレーターのアクスタに「安定」を追加する
- ユーザー依頼: 台座の上側位置を基準に、イラスト横幅の60%位置から斜めに三角になるよう一定距離進み、その後まっすぐ伸ばす底面エリアを追加する
- ユーザー依頼: 底面エリア形成時に途中でイラストアウトラインへ当たった場合は、片側はそこで終わりつなげる
- ユーザー依頼: 底面形成後は通常のツメ生成と同じ処理にする
- ユーザー追加依頼: 底面エリアの開始位置はイラスト底面と同じ高さにし、イラスト底面、底面エリア、台座上部が同じ高さに見えるようにする
- ユーザー追加依頼: 底面エリアの直線部分は横方向ではなく上方向へ伸ばす

### 実装方針

- 既存の `stable` 形状モードをUIに表示し、プレビューAPIへ送信できるようにする
- API側の `stable` では、左右合計でイラスト幅60%になる開始点をイラスト底面Yに置き、中心へ向かう内側上方向の斜線と上方向の直線で底面エリアを作る
- 底面エリアの外周は、斜線到達点を頂点として含め、斜め線の後にそのまま内側へ直線接続されないようにする
- 斜線後の縦線は固定距離ではなくイラスト上端Yまで真上に伸ばし、途中で元イラストに接触した場合だけそこで止める
- イラスト上端Yまで接触しなかった場合のみ、その高さで中心方向へ水平に曲げる
- 安定底面追加後も接地Yはイラスト底面のまま維持し、台座位置が下へずれないようにする
- 斜め距離と開始幅比率はAPI内定数として調整可能にする
- 各辺のトレース中に元イラストマスクへ接触した場合、その側の終点として採用し、反対側とは底辺でつなぐ
- 底面エリアを追加した後は、シンプルと同じツメ生成を実行する

### 変更予定のファイルと理由

- `app/api/acrylic/preview/route.ts`: 安定底面マスク生成とツメ生成分岐を追加するため
- `components/works/AcrylicKeychainTool.tsx`: アクスタの「安定」切替ボタンを追加するため
- `plan.md`: 作業方針を記録するため

### 影響範囲

- アクスタのシンプル/安定プレビュー生成
- アクスタ切替時のプレビューキャッシュと再生成
- アクスタ台座位置とツメ表示

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`

### 懸念点・制約・未確定事項

- 安定底面はプレビューAPIのPNGレイヤー生成に対する実装で、SVG書き出しは既存どおりアクキーのみ対象とする

---

## 追加対応: アクリルプレビュー生成高速化

### 対応する仕様

- ユーザー依頼: アクキーデモページ及びシミュレーターで、プレビュー生成が遅い問題を改善する
- ユーザー依頼: 特にデモページで余白を大きくしたときの遅さを改善する
- ユーザー依頼: 最初の生成はSVG生成処理に合わせているため、隙間埋め処理の結果は変えない
- ユーザー依頼: シミュレーターの固定値では変更前と同じ結果になることを既存比較機能で確認する

### 実装方針

- `/api/acrylic/preview` のマスク膨張処理は、既存と同じ円形半径判定を維持したまま高速な実装へ置き換える
- `fillNarrowTransparentGaps` の前後関係や閾値は維持し、SVG整合に影響する初期形状確定ロジックは変えない
- まずはプレビューAPIの計算量削減に絞り、フロント側の追加最適化は別作業として扱う
- 既存の `npm run acrylic:compare` を使い、固定値生成で baseline と一致することを確認する

### 変更予定のファイルと理由

- `app/api/acrylic/preview/route.ts`: 同一結果を保ったままマスク膨張処理を高速化するため
- `plan.md`: 今回の制約と検証方針を記録するため

### 影響範囲

- アクキー/アクスタのプレビューAPI生成時間
- デモページと通常シミュレーターの再生成待ち時間
- 回帰比較で保存している preview JSON と各レイヤーPNG

### 検証方法

- `npm run acrylic:compare`
- `npm run lint`
- `npx tsc --noEmit --pretty false`
- `npm run build`

### 懸念点・制約・未確定事項

- プレビューAPIとSVG export APIで隙間埋めの意味的整合は維持する必要がある
- 既存比較スクリプトはローカルサーバー起動が前提のため、検証時は開発サーバーを別セッションで動かす

---

## 追加対応: Hanabi 花火モード

### 対応する仕様

- ユーザー依頼: `random_canvas_fireworks_v6_light.html` を参考に、Hanabi作品の背景に花火を表示する
- ユーザー依頼: 下側にテスト用ボタンを置き、通常モードと花火モードを切り替える
- ユーザー依頼: 花火モードでは1発ずつ花火を打ち上げる
- ユーザー依頼: 花火が出ている間だけ `hair-highlight-2.png`、`apple-highlight.png`、`hand-highlight.png`、`highlight-2.png` を重ねる
- ユーザー依頼: 追加ハイライトは即時切替ではなくゆっくりフェードする

### 実装方針

- `HanabiScene` 内に花火用canvasを追加し、星空canvasより前、baseレイヤーより後ろに配置する
- 通常モードでは現状の表示と音声ボタンだけを維持し、花火描画と追加ハイライトは出さない
- 花火モード中はcanvas内でロケット、爆発、粒子を1発分だけ管理し、終了後に次の1発を打ち上げる
- 追加ハイライトは専用レイヤー配列で管理し、CSSのopacity transitionでフェード表示する

### 変更予定のファイルと理由

- `components/portfolio/HanabiScene.tsx`: モード切替、花火canvas、花火状態、追加ハイライトレイヤーを実装するため
- `app/globals.css`: 花火canvas、追加ハイライトのフェード、テスト用モードボタンの見た目を追加するため

### 検証方法

- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
